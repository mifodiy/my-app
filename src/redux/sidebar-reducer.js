let initials = {
    friend: [
        {id: '1', name: 'Sasha'},
        {id: '2', name: 'Petya'},
        {id: '3', name: 'Gera'}
    ]
}

const sidebarReducer = (state = initials, action) => {
return state;
}
export default sidebarReducer;